name: Frontend tests

on:
    push:
        paths:
            - 'frontend/**'
            - '.github/workflows/frontend-*'
    pull_request:
        paths:
            - 'frontend/**'
            - '.github/workflows/frontend-*'

jobs:
    placeholder:
        runs-on: ubuntu-latest
        steps:
            - name: Placeholder step
              run: echo "No tests or linting configured yet - workflow passes by default"
